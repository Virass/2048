export const initialSettings = {
  gameFieldSize: 4,
};

export enum Screen {
  StartScreen = "StartScreen",
  GameField = "GameField",
  Settings = "Settings",
  Preloader = "Preloader"
};

export const colors = {
  bg: "#503486",
  secondBg: "#3d2963",
  text: "#fff",
  opacityText: "rgba(255, 255, 255, 0.5)",
}
